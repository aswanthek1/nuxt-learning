export default defineNuxtRouteMiddleware((to, from) => {
    // console.log(to);
    // console.log(from);
    // to is the destination
    // from is the current path
    // const isLoggedIn = true;
    // if(isLoggedIn) {
    //     return navigateTo(to.fullPath)
    // }
    // return navigateTo("/")
})